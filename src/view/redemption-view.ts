/**
 * // delete /modify before submission
 * The view will be responsible for the user interface, 
 * in this case, console logging the tasks.
 */

export class RedemptionView {

    public initialMessage(): void {
        
    }

    public lookup_response(): void {
        // display response

        this.initialMessage()

    }

    public verification_response(): void {
        // display response

        this.initialMessage()

    }

    public new_redemption_response(): void {
        // display response

        this.initialMessage()
    }
}